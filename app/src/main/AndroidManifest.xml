<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.techtown.sns_project">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SNS_Project">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="Login.LoginActivity" />
        <activity android:name="Login.SignUpActivity" />
        <activity android:name="Login.PassRestActivity" />
        <activity android:name="Login.MemberInfoActivity" />
        <activity android:name=".camera2basic.CameraActivity" />
        <activity android:name=".WritePostActivity" />
        <activity android:name=".ChatActivity" />
        <activity android:name="LibraryInfo.LibraryActivity" />
        <activity android:name="LibraryInfo.LibraryPostActivity" />
    </application>

</manifest>